{
  "name": "polaris-contacts",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "web": "pnpm --filter @contacts/web dev",
    "desktop": "pnpm --filter @contacts/desktop dev",
    "server": "pnpm --filter @contacts/server dev"
  },
  "engines": {
    "node": ">=18"
  }
}
